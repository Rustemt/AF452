@model List<Nop.Web.Models.ShoppingCart.WishlistModel.ShoppingCartItemModel>

         @if (Model.Count < 1)
         {
            @T("Myaccount.Overview.WishList.EmptyMessage")
         }
        else
               {

               foreach(var item in Model)
               {
                    	@Html.Partial("WishListSummaryProduct",item)
               }
                
               
                }

            

           
